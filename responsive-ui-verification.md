# 全站手機響應式驗證記錄

目前完成的 responsive 變更：

- 全局 html、body、#root 限制為 100% 寬度並隱藏頁面級橫向溢出。
- 導航按鈕在窄螢幕自動兩欄換行並保持可點擊寬度。
- 事件 Modal 的日期／類型欄位在手機改為單欄；新增／編輯操作按鈕在手機改為垂直排列。
- 重複活動區塊及 Modal 內容限制最小寬度，避免窄螢幕撐開頁面。
- 器材借用 iframe 在手機使用 viewport-relative 高度，桌面維持較高顯示高度。

瀏覽器檢查：

- 預覽頁成功載入，現有登入狀態、月曆、活動清單、成員管理、器材借用及通知入口仍可見。
- 頁面 HTML 未出現新增錯誤；開發伺服器保持 running。

自動化測試：

- responsive-layout：3/3 通過。
- equipment-loan：1/1 通過。
- repeat-dates：4/4 通過。
