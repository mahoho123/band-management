# 重複活動日期多選 UI 驗證記錄

日期：2026-08-12

已用副主席登入成功，並在現有「新增活動」彈窗啟用「重複活動」。彈窗內顯示「點擊選擇日期」按鈕；點擊後成功開啟「選擇重複日期」日曆面板，顯示 2026 年 8 月、星期標題、1–31 日期、上一月／下一月控制，以及「確定」「取消」按鈕。整個功能仍在既有新增活動 Modal 內，未改動其他頁面。

下一步：點擊未選日期、再次點擊同一日期確認 Toggle 取消，選取多個日期後按「確定」，檢查下方列表及提交陣列。

瀏覽器結果：

1. 選取日期 2 後，按鈕即時顯示「已選擇 1 個日期」。
2. 再次點擊日期 2 後，按鈕即時恢復「點擊選擇日期」，確認 Toggle 取消有效。
3. 再選取日期 3 和日期 4 後，按鈕即時顯示「已選擇 2 個日期」，確認多選有效。

待完成：按「確定」確認日期批次帶入下方列表。

4. 按「確定」後，列表成功带入 `2026-08-03` 和 `2026-08-04` 两行，X 删除按钮保留；提交按钮即时显示「創建 3 個活動」（主日期 + 2 个重复日期）。
5. 点击第一行 X 后，该日期被移除，列表只剩 `2026-08-04`，提交按钮即时更新为「創建 2 個活動」。

UI 驗證完成。
